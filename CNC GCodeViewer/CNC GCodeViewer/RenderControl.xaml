<UserControl x:Class="CNC.Controls.Viewer.RenderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:CNC.Controls;assembly=CNC.Controls.WPF"
             xmlns:local="clr-namespace:CNC.Controls.Viewer"
             mc:Ignorable="d"
             Loaded="RenderControl_Loaded"
             d:DesignHeight="335" d:DesignWidth="400">
    <UserControl.Resources>
        <Controls:HomedStateToBooleanConverter x:Key="IsHomed" />
        <Controls:LogicalNotConverter x:Key="IsNotRunningConverter" />
        <Controls:LogicalNotConverter x:Key="IsNotVisibleConverter">
            <Controls:LogicalNotConverter.FinalConverter>
                <Controls:BoolToVisibleConverter />
            </Controls:LogicalNotConverter.FinalConverter>
        </Controls:LogicalNotConverter>
        <BitmapImage x:Key="ResetViewIcon" UriSource="pack://application:,,,/CNC.Controls.Viewer;component/Resources/ResetView.png" />
        <BitmapImage x:Key="RestoreViewIcon" UriSource="pack://application:,,,/CNC.Controls.Viewer;component/Resources/RestoreView.png" />
        <BitmapImage x:Key="GridOnIcon" UriSource="pack://application:,,,/CNC.Controls.Viewer;component/Resources/GridOn.png" />
        <BitmapImage x:Key="GridOffIcon" UriSource="pack://application:,,,/CNC.Controls.Viewer;component/Resources/GridOff.png" />
        <BitmapImage x:Key="JobBoxOnIcon" UriSource="pack://application:,,,/CNC.Controls.Viewer;component/Resources/JobBoxOn.png" />
        <BitmapImage x:Key="JobBoxOffIcon" UriSource="pack://application:,,,/CNC.Controls.Viewer;component/Resources/JobBoxOff.png" />
        <BitmapImage x:Key="WorkBoxOnIcon" UriSource="pack://application:,,,/CNC.Controls.Viewer;component/Resources/WorkBoxOn.png" />
        <BitmapImage x:Key="WorkBoxOffIcon" UriSource="pack://application:,,,/CNC.Controls.Viewer;component/Resources/WorkBoxOff.png" />
        <BitmapImage x:Key="XYZOnIcon" UriSource="pack://application:,,,/CNC.Controls.Viewer;component/Resources/XYZOn.png" />
        <BitmapImage x:Key="XYZOffIcon" UriSource="pack://application:,,,/CNC.Controls.Viewer;component/Resources/XYZOff.png" />
        <BitmapImage x:Key="SaveViewIcon" UriSource="pack://application:,,,/CNC.Controls.Viewer;component/Resources/SaveView.png" />
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Margin" Value="3,0,0,0"/>
        </Style>
        <Style x:Key="Flag" TargetType="ToggleButton">
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ToggleButton">
                                <Image Source="{Binding Path=Content, RelativeSource={RelativeSource TemplatedParent}}"/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsChecked" Value="False">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ToggleButton">
                                <Image Source="{Binding Path=Tag, RelativeSource={RelativeSource TemplatedParent}}"/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Margin" Value="3,0,0,0"/>
        </Style>
    </UserControl.Resources>
    <DockPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" HorizontalAlignment="Right">
            <ComboBox IsEnabled="{Binding Path=IsJobRunning, Converter={StaticResource IsNotRunningConverter}}" Visibility="{Binding Path=LatheModeEnabled, Converter={StaticResource IsNotVisibleConverter}}" ItemsSource="{Binding Path=MachineView.RenderModes, RelativeSource={RelativeSource AncestorType=UserControl}}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding Path=MachineView.RenderMode, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" Width="50"/>
            <ComboBox Margin="3,0,0,0" ItemsSource="{Binding Path=MachineView.ToolModes, RelativeSource={RelativeSource AncestorType=UserControl}}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding Path=MachineView.ToolMode, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" Width="80"/>
            <Button x:Uid="resetView" Style="{StaticResource IconButton}" Click="ResetView_Click" ToolTip="Reset view (Ctrl+V)">
                <Image Source="{StaticResource ResetViewIcon}"/>
            </Button>
            <Button x:Uid="restoreView" Style="{StaticResource IconButton}" IsEnabled="{Binding Path=MachineView.CanRestoreView, RelativeSource={RelativeSource AncestorType=UserControl}}" Click="RestoreView_Click" ToolTip="Restore view (Ctrl+R)">
                <Image Source="{StaticResource RestoreViewIcon}"/>
            </Button>
            <ToggleButton x:Uid="showGrid" Content="{StaticResource GridOnIcon}" Tag="{StaticResource GridOffIcon}" Style="{StaticResource Flag}" IsChecked="{Binding Path=MachineView.ShowGrid, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" ToolTip="Toggle grid visibility (Ctrl+G)"/>
            <ToggleButton x:Uid="showJobEnvelope" Content="{StaticResource JobBoxOnIcon}" Tag="{StaticResource JobBoxOffIcon}" Style="{StaticResource Flag}" IsChecked="{Binding Path=MachineView.ShowJobEnvelope, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" ToolTip="Toggle job bounds visibility (Ctrl+J)"/>
            <ToggleButton x:Uid="showWorkEnvelope" Content="{StaticResource WorkBoxOnIcon}" Tag="{StaticResource WorkBoxOffIcon}" Style="{StaticResource Flag}" IsChecked="{Binding Path=MachineView.ShowWorkEnvelope, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" IsEnabled="{Binding Path=HomedState, Mode=OneWay, Converter={StaticResource IsHomed}}" ToolTip="Toggle machine work envelope visibility (Ctrl+W)"/>
            <ToggleButton x:Uid="showXYZMarkers" Content="{StaticResource XYZOnIcon}" Tag="{StaticResource XYZOffIcon}" Style="{StaticResource Flag}" IsChecked="{Binding Path=MachineView.ShowAxes, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" ToolTip="Toggle axes visibility"/>
            <Button x:Uid="saveView" Style="{StaticResource IconButton}" IsEnabled="{Binding Path=IsFileLoaded}" Click="SaveView_Click" ToolTip="Save view">
                <Image Source="{StaticResource SaveViewIcon}"/>
            </Button>
        </StackPanel>
        <Grid DockPanel.Dock="Top">
        <local:Renderer x:Name="gcodeView"/>
        <local:TextOverlayControl x:Name="textOverlay" HorizontalAlignment="Right" Margin="5,5" VerticalAlignment="Top"/>
        </Grid>
    </DockPanel>
</UserControl>
