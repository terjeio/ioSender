<UserControl x:Class="CNC.Controls.GcodeTextControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
             xmlns:local="clr-namespace:CNC.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <local:LogicalNotConverter x:Key="NotConverter" />
    </UserControl.Resources>
    <Grid Background="Black">
        <avalonedit:TextEditor x:Name="Editor"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Stretch"
                               AllowDrop="True"
                               DragEnter="Editor_Drag"
                               DragOver="Editor_Drag"
                               DragLeave="Editor_Drag"
                               Drop="Editor_Drop"
                               FontFamily="Consolas"
                               FontSize="24"
                               BorderThickness="0"
                               HorizontalScrollBarVisibility="Auto"
                               VerticalScrollBarVisibility="Auto" ShowLineNumbers="True" IsReadOnly="{Binding Path=IsFileEditing,
                     Converter={StaticResource NotConverter},
                     Mode=OneWay}">
            <avalonedit:TextEditor.ContextMenu>
                <ContextMenu x:Name="ctxMenu">
                    <MenuItem x:Uid="mnu_sendToController" Header="Send to controller" IsEnabled="{Binding Path=MultipleSelected}" Click="SendController_Click" />
                    <MenuItem x:Uid="mnu_startFromHere" Header="Start from here" IsEnabled="{Binding Path=SingleSelected}" Click="StartHere_Click" />
                    <MenuItem x:Uid="mnu_copyToMDI" Header="Copy to MDI" IsEnabled="{Binding Path=SingleSelected}" Click="CopyMDI_Click" />
                </ContextMenu>
            </avalonedit:TextEditor.ContextMenu>
        </avalonedit:TextEditor>
    </Grid>
</UserControl>
