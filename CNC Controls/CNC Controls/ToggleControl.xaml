<UserControl x:Class="CNC.Controls.ToggleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CNC.Controls"
             mc:Ignorable="d"
             d:DesignHeight="26" d:DesignWidth="100">
    <UserControl.Resources>
        <!-- Pill-style toggle template that mimics the look of the previous
             HorizontalToggleSwitch without depending on the third-party library. -->
        <Style x:Key="PillToggleSwitchStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Focusable" Value="True"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Grid>
                            <Border x:Name="Track"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    SnapsToDevicePixels="True"/>
                            <Ellipse x:Name="Thumb"
                                     Width="16" Height="16"
                                     HorizontalAlignment="Left"
                                     Margin="2,0,0,0"
                                     Fill="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
                                     Stroke="{TemplateBinding BorderBrush}"
                                     StrokeThickness="1">
                                <Ellipse.RenderTransform>
                                    <TranslateTransform x:Name="ThumbTransform" X="0"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetName="ThumbTransform"
                                                Storyboard.TargetProperty="X"
                                                To="20"
                                                Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetName="ThumbTransform"
                                                Storyboard.TargetProperty="X"
                                                To="0"
                                                Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                                <Setter TargetName="Track" Property="Background" Value="#2D89EF"/>
                                <Setter TargetName="Track" Property="BorderBrush" Value="#2D89EF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Track" Property="Opacity" Value="0.5"/>
                                <Setter TargetName="Thumb" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="7*"/>
        </Grid.ColumnDefinitions>
        <ToggleButton x:Name="tsw"
                      Style="{StaticResource PillToggleSwitchStyle}"
                      IsChecked="{Binding Path=IsChecked, Mode=OneWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      Margin="0,3,0,0" Height="20" Width="40"
                      HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <Label x:Uid="lbl_ToggleSwitch" Grid.Column="1" Content="{Binding Path=Label, RelativeSource={RelativeSource AncestorType=UserControl}}" Height="26" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
    </Grid>
</UserControl>
